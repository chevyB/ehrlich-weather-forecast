const Root = (): React.ReactElement => {
  return (
    <div
      className="test test-test test-3sdfsdf test testset"
      id="testset"
      onClick={() => null}
    >
      24 Hours Code Challenge | Weather Forecast
    </div>
  );
};

export default Root;
